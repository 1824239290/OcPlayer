# 2026-08-29 全项目优化点 Review

覆盖 2026-08-24 上轮 review 之后的全部新增代码（约 7000 行：弹幕、Emby 适配、iOS 手势、
更新检查、内存治理、首页重设计等）。方法：5 个分区并行审查（App 核心层 / 播放器+详情 UI /
内容浏览 UI / 网络四包 / 播放弹幕包+构建链路），共 **161 条发现：P0×1、P1×17、P2×59、P3×84**。
标注 ✅ 的条目已由主会话逐行核验过代码。已知豁免（Gitea token、--clobber、凭据明文
UserDefaults、AppSecret 分发、fork 内核单点）未重复列入。

> **修复进度（2026-08-29）：P0 与 P1 全部 17 条已修入库**（各项标了 commit 短码），
> macOS 83 测试 + 四个改动包测试 + iOS 构建全绿。P2×59 / P3×84 待做。
>
> **P2 阶段 1（2026-08-29 晚）已修 11 条**（并发与数据正确性整组 + signOut 补清）：
> MoviePilot 流式乱序/重入（2791aa4）、MP 首页换服缓存/搜索重入（5659662）、
> Bangumi chip 双搜/loadMore 代次（ec070e7）、iOS 手势换指+seek 去抖（3a51d52）、
> PlayerState 同值去重（00af4ea）、RenderLoop 泄漏池/detach 补断（1579d46）、
> Jellyfin 取消透传（ec9a8e5）、MP 重登看门狗+熔断（0c44fb9）、
> 登出放手 reporter（8596020）。
>
> **P2 阶段 2（2026-08-29 深夜）已修 12 条**（主线程与性能组里的播放器/弹幕/Erika 半区）：
> panSession 子树拆分/hover 单通道/键盘白名单/单双击窗口（70ba260）、
> 音量落盘去抖+2x 不持久化（b6437bc）、overlay 采样自适应（e041df6）、
> 入池清位图+轨道尺寸守卫（caa3924）、Erika idle 档/resize 日志/事件上限（9a642e1）、
> stopPlayback 收口补写音量（09d1e65，复核补漏）。
>
> **P2 阶段 3（2026-08-29 深夜）已修 15 条**（UI 数据性能与缓存整组）：
> 资源列表单次排序、ServerStore 进程内缓存、DanmakuCache mapping 缓存+过期即删+
> pruner 保护 mapping、网关单遍解码+默认 session 超时、脱敏后置+summary 挪队列、
> DatabasePool、详情页 SWR 快照/按季回填/横幅 GeometryReader+渐变精简、
> libraryPages 上限、字幕维护钩子。
> **缓办 1 条**：「当前请求」身份 6 处簿记收敛——7 个字段在 open/close/换片/出错/
> 重试全路径交织（~20 处转换点），属结构性重构，需要专门一轮逐转换点核对，
> 不宜在长阶段尾部赶工；单独立项。
> **口径修正 1 条**：ExternalSubtitles「只增不删」不成立——下载目录就是
> importedSubtitles，日定时器 + 导入钩子都在清；本轮只补了下载后的即时触发。
> **暂缓 1 条**：DanmakuAsyncLayer 上下文缓存池——16 条绘制队列并发要加锁、
> cell 尺寸离散导致命中率低（需量化分桶）、iOS 路径要连 UIGraphics 语义一起重写、
> 大块 calloc 本就被 libmalloc 摊销，收益配不上 vendored 渲染路径的回归风险；
> 若要做需专门立项（量化分桶 + CTM/缓冲重置 + iOS 直用 CGBitmapContext）。
>
> **增补 #18（2026-08-29 晚，真机演示时新发现，已修）：seek 越过 EOF → 内核错误风暴 → 播放器假死（P1）**
> `PlaybackController.skip(by:)` 不把目标位置 clamp 在片长内，键盘 ⏭ 连按可把进度顶过片长
> （实测 72 连按到 27:06 / 24:00）；内核随即进 `audio output stalled at EOF` 状态并逐帧重发
> `.failed` 事件，`PlayerState.apply` 无条件记日志+写状态，一次刷了 **6302 条**重复错误、
> 主线程被轰炸、画面冻结假死（进程未退）。修复：skip 目标 clamp 到 `duration - 0.5s`；
> `.failed` 相同 code+message 只记一次（`lastFailedEventKey`，reset 时清）。
> 深层修复（EOF stall 本身）在内核 fork 侧，另行立项。

---

## 一、P0（1 条）

1. **P0 ✅ 已修（0a52786）** `App/Shared/Features/Player/DanmakuOverlay.swift:144` — `UInt32(item.color ?? …) & 0xFF_FF_FF`：
   mask 在转换**之后**，弹幕 JSON 的 color 为负数时 `UInt32(负Int64)` 直接 trap 崩溃。
   修：先 `max(0, min(v, 0xFFFFFF))` 再转。

## 二、P1（17 条，按影响排序）

### 会话 / 服务器
2. ✅ **iOS ATS 双 key 复活（已修 3b2017f）** — `OcPlayer.xcodeproj/project.pbxproj:492,519`（iOS target Debug+Release）
   仍注入 `INFOPLIST_KEY_NSAppTransportSecurity_NSAllowsLocalNetworking = YES`，与 `Config/Info.plist`
   的 `NSAllowsArbitraryLoads` 并存——正是 Info.plist 注释里警告的「后者被忽略」组合，iOS 公网 HTTP
   源会被 ATS 拦死。修：删这两行 build setting。上轮修了 plist 没修 pbxproj。
3. ✅ **设置页「切换服务器」对无 token 档案是死路（已修 1bb63d3）** — `AppModel+Session.swift:33` + `:224-225`：
   `switchToServer` 走死 token 分支时先 `store.signOut(id:)` 删掉 token，再调 `connectServer`，
   但其 `guard … phase == .onboarding` 在 `.ready` 态直接 return——探活结果丢弃、无任何 UI 反馈，
   且 token 已被删。修：死 token 分支先置 `phase = .onboarding`（或走 `reconnectFlow()`）。
4. **切换/重连不停播放（已修 1bb63d3）** — `AppModel+Session.swift:135-148`：`dropSessionData` 只清 UI 引用，
   不停引擎不 `cancelPlaybackOpen`；「播放中→设置→连接其它服务器→登录另一台」会留下无 UI、
   仍在出声的播放会话。修：抽公共 teardown（signOut/completeLogin/switchToServer 三处共用）。
5. **换号后增量同步漏数据（已修 b3ea5de）** — `BangumiKit/Client/BangumiStore.swift:109-116` + `BangumiContext.swift:287-291`：
   `clearAccountState()` 定义了但全仓无调用方，登出不清 `collectionsUpdatedAt`，新账号带着旧时间戳
   做增量同步会漏拉收藏。修：登出/新登录时调用并重置时间戳。

### 播放器 / 弹幕
6. ✅ **暂停态弹幕自动恢复滚动（已修 74305fb）** — `DanmakuRenderKit/Classes/Core/DanmakuView.swift:621-627`：
   `update()` 无条件 `pause()`→`asyncAfter(0.01)`→`play()`，暂停中改弹幕速度会误恢复播放，
   连续改速叠加多个 play 定时器反复重排。修：闭包前记住旧 status，仅原来是 .play 才延时恢复 + token 防叠加。
7. **章节保底跳过无去重，按钮原地循环（已修 3e14507）** — `PlaybackChapter.swift:241-247` + `PlaybackController.swift:655-660`：
   片尾保底跳过不记 `skippedIDs`，跳到「片尾前 20s」仍在 90s 窗口内，再按无效。修：保底跳过也入 skippedIDs。
8. **HUD 弹幕「时间偏移」整块 UI 永不显示（已修 ae29271）** — `PlayerHUDActions.swift:318` + `PlaybackController+Tracks.swift:32-49`：
   当前恒走 overlay 路线，`replaceDanmaku` 在 overlay 分支提前 return，`danmakuTracks` 恒空，
   显隐条件绑在它上面的时间偏移 UI 不可达。修：overlay 路线也写代表项或改绑 `danmakuModel.danmaku.status`。
9. **iOS 手势中断收尾缺失（已修 33c9b7d）** — `PlayerScreen.swift:364-403`：长按 2x 期间切后台/来电，holdTask 挂起，
   回前台后仍会 `beginHoldFastForward()`（macOS 有 didResignActive 兜底 :258，iOS 无对应）。
   修：iOS 加 `.onChange(of: scenePhase)` 后台时 cancel + endHoldFastForward + 收尾 panSession。

### 功能断线
10. **「找片下载」查询词从未传给 MoviePilot（已修 d14b277）** — `BangumiSubjectDetailView.swift:246-251`：
    算出 query 后只切 tab，全仓无 pending-search 通道，与注释宣称的「按番名搜索」不符。
    修：AppModel 加 `pendingMoviePilotQuery`，MoviePilotHomeView `.task` 消费后清空。
11. **远程哈希逐字节 await（已修 ccf6073）** — `DanmakuKit/FileHash.swift:91-99`：`for try await byte` 逐字节跳 + 逐字节
    append，16 MiB = 1600 万次异步跳，弹幕远程指纹慢几个数量级。修：Range 头拉取或 64KiB 块读。

### 网络包
12. **EmbySanitizer 对所有响应无条件全量三遍（已修 ee09994）** — `JellyfinKit/JellyfinServer.swift:595` + `LooseItems.swift:49-55,116-118`：
    JSONSerialization 解析→递归深拷贝重建→再序列化→LooseDecoding 再解析，纯 Jellyfin 服务器也照付，
    Items 大页翻倍延迟与内存峰值。修：仅 `kind == .emby` 或首次解码失败时走 sanitizer 回退。
13. **Bangumi 建库在主 actor（已修 b3ea5de）** — `BangumiKit/Client/BangumiContext.swift:239-252`：文件 IO + SQLite 迁移整个
    `Task { @MainActor in … }`，启动期卡 UI。修：后台建库后跳回主 actor。
14. **JellyfinKit 全包零超时（已修 ee09994）** — `JellyfinKit/JellyfinServer.swift:99,108-121`：默认 request 60s、resource 7 天，
    浏览/PlaybackInfo 卡死时 UI 干等一分钟。修：显式 request≈15-30s、resource≈60s。
15. **BangumiURL 强解包（已修 b3ea5de）** — `BangumiKit/Client/BangumiURL.swift:77`（另 :125,139,410）：
    `URL(string: "https://\(domain)\(path)")!`，mirror 域名来自用户 UserDefaults，异常输入即崩溃。
    修：可失败初始化 + 抛错误。

### CI
16. **CI skip 列表漏新套件（已修 2774e1a）**（✅ 修正后口径）— `.github/workflows/test.yml:87-88`：skip 列表是 08-24 定的，
    之后新增的 `ErikaReadAheadBehaviorTests`（`ErikaKit/Tests/…/ErikaReadAheadBehaviorTests.swift:100`
    会 `try ErikaPresenter()`）不在 skip 列表，GPU-less runner 上有 hang 风险。修：补 skip。
    （`ErikaSmokeTests` 本身存在且已在列表，无问题。）
17. **release 不等 test（已修 2774e1a）** — `.github/workflows/release.yml:87-89`：tag push 直接触发 Release 构建+发布，
    与 Test 工作流无 `needs` 依赖，未测过的 commit 可直接出包。修：release job 加 `needs: test` 或同 workflow 先跑包测试。

## 三、P2（59 条，按主题分组）

### 并发与数据正确性（13 条整组已修 ✅ 2026-08-29 晚；短码对照见顶部进度注记）
- `MoviePilotResourceView.swift:360-366` — 流式回调经无序 `Task { @MainActor }` 回主线程，同代际乱序时旧批次覆盖新批次；改 AsyncStream/序号丢弃。
- `MoviePilotResourceView.swift:344` — `guard !isSearching` 使上次全站搜索未结束时新搜索被静默丢弃，无取消入口；允许重入按 generation 取消旧流。
- `MoviePilotHomeView.swift:191-195` — 改绑另一台 MP 服务器后 `subscribes`/`localSubscribedMediaIDs` 不清不重拉，残留旧服务器订阅。
- `MoviePilotHomeView.swift:552-554` — 搜索在途提交新关键词被 `guard !isSearching` 静默吞掉。
- `BangumiHomeView.swift:345-353` + `:140-145` — 分类 chip 触发 onChange 搜索、chip 自己又起 Task 再搜一次，双份请求互竞代次。
- `BangumiCollectionListView.swift:88-97` — `loadMore` 无代次守卫，切收藏类型时在途旧翻页 append 进新列表。
- `PlayerScreen.swift:365-368` — `DragGesture(minimumDistance:0)` 单触点，第二根手指落下触发 onEnded：横滑 seek 误 commit、滑动误判 tap，无触点数守卫。
- `PlayerScreen.swift:371-387` — 被系统打断的触摸 2s 内不判 stale，恢复后复用旧 touchStart/panSession，亮度/音量从旧值跳变。
- `PlaybackKit/PlayerState.swift:143-158` — `apply` 对相同值不去重，内核逐帧重发 stateChanged 造成重复 UI 通知。
- `ErikaKit/RenderLoop.swift:57-62` — `stop()` 超时后 `self.thread = nil` 释放仍在运行的 RenderThread（崩溃风险）；超时应挂泄漏池。
- `ErikaKit/VideoSurfaceView.swift:108` — `passUnretained(layer)` 裸指针交内核后 detach 抛错则悬垂；detach 失败记标志并在 deinit 强制断开。
- `JellyfinKit/JellyfinError.swift:56-66` + `JellyfinServer.swift:598-601` — 取消被包成 `.transport`/`.other`，UI 会把用户取消弹成错误框。
- `MoviePilotAPIClient.swift:369-414` — 静默重登无超时看门狗（Bangumi 侧有 15s），重试循环每 attempt 可再发带密码 login（最多 3 连发）；需 single-flight+熔断。

### 主线程与性能（播放器/弹幕/Erika 半区 13 条：阶段 2 已修 12 条 ✅ 2026-08-29 深夜，DanmakuAsyncLayer 上下文池暂缓——理由与立项条件见顶部注记；其余条目属阶段 3）
- `PlayerScreen.swift:46,162-183` — `panSession` 被 body 直接读，拖动中每个 onChanged（60-120Hz）让整棵 PlayerScreen 重算（含整套 Glass 树）；拆只读子视图或挪 @Observable 模型。
- `PlayerScreen.swift:82-88` 与 `222-232` — macOS 鼠标移动双通道：NSTrackingArea 与 `.onContinuousHover` 同时安装，同一事件走两遍；删其一。
- `PlayerScreen.swift:667-728` — macOS 全局键盘监听无修饰键全拦截，弹幕 sheet/fileImporter 打开时 ESC 关掉整个播放器；keyWindow 非 mainWindow 时放行。
- `PlaybackController.swift:41-47` + `PlayerScreen.swift:468` — 音量纵滑/Slider 每 tick `applyVolume` → UserDefaults 同步写（每秒最多百次磁盘写）；松手再落盘。
- `PlaybackController.swift:765-776` — `beginHoldFastForward` 把临时 2.0 持久化进 PlaybackPreferences，加速中杀 App 下次启动默认 2 倍速。
- `PlayerScreen.swift:69-70,504-525` — `singleTapDelay`(320ms) < `doubleTapInterval`(350ms)，320-350ms 区间双击暂停永远丢失。
- `DanmakuOverlay.swift:186-199` — 60Hz Timer 在暂停/buffering/禁用/空数据时仍每秒唤醒主线程 60 次；按状态 stop/start。
- `DanmakuAsyncLayer.swift:102-184` — 每个弹幕 cell 每次 redraw 新建全尺寸 CGContext 位图；按 size+scale 做上下文缓存池。
- `DanmakuView.swift:855-866` — cell 入池时 layer 仍持有上次渲染位图 contents，复用池白占内存；入池前 `contents = nil`。
- `DanmakuView.swift:276-284` + `DanmakuViewAdapter.swift:126-135` — frame KVO/每次 layout 全量 `recalculateTracks()`；仅 bounds.size 实际变化时重算。
- `ErikaEngine.swift:437-439` — 只在 paused 降帧 30fps，stopped/error 后仍全刷新率空转；加 idle/ended 档或停 link。
- `ErikaEngine.swift:125-129` — `resize` 用 `try?` 吞内核错误，失败后黑屏无日志；至少 PlaybackLog.error。
- `ErikaEngine.swift:414-431` — 事件抽干锁内 `while true` 无上界，事件风暴时饿死 UI 控制调用；加每帧迭代上限。
- `MoviePilotResourceView.swift:137-143` — `displayedTorrents` 每次 body 对全量过滤+排序，流式期间每批回调触发一次 O(n log n)；缓存在 @State。
- `DetailView.swift:112,942-984` — 详情页每次进入全量重拉 item/seasons/similar 且置 nil 闪骨架屏；做 stale-while-revalidate。
- `DetailView.swift:987-1009` — `reloadAfterPlayback` 清空整个 `episodesBySeason` 只回填当前季；用 applyPlayState 按集更新。
- `DetailView.swift:255-265,460-470` — 横幅 GeometryReader 冗余（aspectRatio+frame+clipped 已等价）。
- `DetailView.swift:268-279,473-491` — 横幅上 3 层全尺寸 LinearGradient 每帧合成；合并单条多 stop。
- `ServerStore.swift:68-75/115-124`（两包同名）— `profiles`/`currentProfile` 每次访问都从 UserDefaults 全量 JSONDecoder 解码，每次重渲染主线程解码；进程内缓存。
- `DanmakuCache.swift:81-94` — `loadMapping()` 每次查询整文件读盘+解码，无内存缓存；actor 内缓存写穿。
- `GatewayClient.swift:287-296` — 成功路径先整包 decode(ResponseBase) 再 decode(T.self)，comments（数 MB）总解析两遍。
- `PlaybackController.swift:127-142` + `AppModel+Playback.swift:176-186` — 「当前请求」身份在 6 处簿记靠手工对齐，`openIfNeeded:337` 静默丢弃不符请求；收敛单一入口。
- `DiagnosticLogger.swift:181-185` — 红actor（5 趟正则）在节流判定之前执行，被压掉的热点日志照付全量成本。
- `DiagnosticLogger.swift:488-503` — `summary()` 在 queue.sync 内逐字节扫 2MB 日志且跑在调用线程（设置页主线程）。
- `DatabaseFactory.swift:16` — DatabaseQueue 未开 WAL，章节同步批量写期间读全部排队；换 DatabasePool 或补 WAL pragma。

### 网络健壮性
- `JellyfinServer.swift:588-602` — Jellyfin 侧完全无 401 处置路径（对比 MP silentRelogin），token 失效后持续裸报 `.unauthorized`；至少发通知引导重登。
- `BangumiAPIClient.swift:179-185` ↔ `MoviePilotAPIClient.swift:253-257` — 两份重试退避固定 1s/2s 无抖动；抽共享 helper 加抖动。
- `BangumiAPIClient.swift:244-253` — 429 不读 `Retry-After` 头、固定短退避。
- `BangumiError.swift:119-131` ↔ `MoviePilotError.swift:119-128` — `isRetryable` 靠**中文字符串相等**判断，改文案即静默破坏重试；改枚举承载。
- `LooseItems.swift:128-157` — 日期只认两条固定 DateFormatter（`+08:00` 偏移写法炸整包解码），且 decoder 每请求新建、每日期字段跑两遍 formatter；换静态 ISO8601DateFormatter 双 fallback。
- `BangumiDatabaseOperator.swift:768,776-777` — `LIKE ?` 的 `%/_` 转义了但 SQL 未加 `ESCAPE '\'`，SQLite 默认无转义符，含 %/_ 关键词结果错误。

### 设置与状态
- `AppModel+Session.swift:161-195` vs `:135-148` — signOut 与 dropSessionData 两份几乎相同的手工清理清单已漂移（signOut :171-174 重复 cancel 刚清过的 task）；合并 `resetBrowseState()`。
- ✅ 已修（8596020）`AppModel.swift:202-203` — signOut 未清 `playbackReporting`/`pendingPlaybackReportingHandoff`，coordinator 的 reporter 强引用旧 server（含 token）——唯一实锤的漏清字段组。
- `SettingsView.swift:229-234` — 删除已保存服务器（连 token）无确认弹窗。
- `SettingsView.swift:21,185-187` — 预读设置靠本地 @State+onChange 写回，`PlaybackPreferences` 非可观察，别处改了 Picker 停旧值。
- `AppUpdateChecker.swift:147-151,175,193-197` — GitHub 仓库硬编码且 404 被解释为「已是最新」；repo 挪 Info.plist、404 记日志。
- `RemoteImage.swift:374-391` — `.task(id:)` key 含 authHeader/maxPixelSize 但守卫只比 URL，同 URL 换尺寸/凭证跳过重载留错误尺寸旧位图。
- `AppModel+Playback.swift:535,573,584` — 连播窗口/外挂字幕/字幕下载三处 `try?` 吞网络错误零日志。
- `AppModel+Playback.swift:195-241` — `schedulePreparationDismiss` 100ms 忙轮询 + sleep 忽略取消最长多等 400ms；改回调驱动。
- `AppModel+Playback.swift:244-249` — `playNextEpisode()` 全仓无调用点（HUD「Continue Watching」未接线），死代码或接线。
- `BangumiProfileView.swift:156-168` — 两次请求 `try?`，失败静默显示「暂无收藏」，无错误态重试。
- `BangumiCalendarView.swift:113,374-379` — 每次进入全量重拉日历无 TTL 缓存。
- `BangumiMatcher.swift:398-428` — 关键词循环 `try?` 吞全部网络错误，断网静默返回 nil 误导「没找到」；且 3+3 次串行 await 可并行。
- `BangumiMatcher.swift:217-221` — `hasSuffix("S"/"T")` 把任何 s/t 结尾标题判成第 2/3 季（Arknights 类），整季误绑。
- `CoreModel/MediaItem.swift:5` — 合成 Hashable 覆盖 ~25 个字段（含 overview 长文），大库 diff 代价高；自定义 hash 只用 id+关键显示字段。

### 缓存与存储
- `DanmakuCache.swift:113-129` — 过期 comments 文件从不删除（TTL 只影响读取），App 层 pruner 还会把「永久」mapping.json 当普通文件删；过期即删 + pruner 排除 mapping。
- `GatewayClient.swift:135` + `FileHash.swift:43` — 默认 `.shared` 会话绕过 makeSession() 的 15s/30s 超时；默认参数改用 makeSession()。
- `JellyfinKit/ExternalSubtitles.swift:93-105` — 字幕下载目录只增不删，ManagedDirectoryPruner 在手边没接。
- `AppModel.swift:86` — `libraryPages` 会话内无上限增长；LRU 或内存告警清空。

### 脚本与构建
- `project.pbxproj:453,475,500,527` + `package-macos.sh:6` — `MARKETING_VERSION=0.1.5` 硬编码四份+脚本默认 tag，三处版本源漂移；收敛 App.xcconfig。
- `package-macos.sh:66-67,185-186` — 先 `codesign --deep --sign -` 再重签，第一次纯浪费；删。
- `test.yml:38-49` — 只缓存 Erika 二进制，SPM/DerivedData 每次全量冷跑；加 actions/cache（key 含 Package.resolved hash）。
- `test.yml:20-23` 与 `release.yml:25-29` — Erika env/resolve/cache/fetch 四步两个 workflow 逐字重复；抽 composite action。

> **P2 阶段 4（2026-08-30）已修 6 条**（网络健壮性整组）：
> Jellyfin 401 通知+重登引导（包内 authenticationRequired 通知带 profileID，
> App 侧 phase/.ready + profileID 去重，与换服死 token 同口径）、isRetryable
> 枚举承载（两包 network 载荷改 NetworkErrorClassifier.Kind，Kind 补 Equatable）、
> 429 读 Retry-After（rateLimited case，退避封顶 60s）、两包退避加 0.5~1.5 抖动、
> LooseItems 静态 ISO8601 双 fallback（ISO8601DateFormatter 热路径 + 带冒号偏移
> 兼容 + 无冒号兜底，decoder 不再每请求新建）、LIKE 查询补 ESCAPE '\\'。

> **P2 阶段 5（2026-08-30）已修 13 条**（设置与状态整组）：
> resetBrowseState 合并（两份漂移清单归一）、删服务器确认弹窗、预读档位
> @AppStorage 可观察绑定、更新检查 repo 挪 Info.plist + 404 记日志、
> RemoteImage 复合加载键、连播/字幕三处 try? 补日志、撤 loading 等待可取消 +
> request 归属复核、playNextEpisode 死代码删除、收藏页错误态重试、
> 日历 30 分钟 TTL（force 绕过）、Matcher 关键词并行 + 全失败抛错、
> 季尾字母续作判定收紧（CJK/分隔符边界，Arknights 不再误绑）、
> MediaItem 自定义 hash（== 保持全字段）。
> **部分完成 1 条**：schedulePreparationDismiss 回调驱动——本轮修复了取消
> 吞没与迟到清理两个实缺陷；彻底回调化需要引擎补首帧事件
>（PlayerEvent + ErikaEngine 发射 + PlayerState 折叠），随内核侧立项一并做。

## 四、P3（84 条，速列）

**App 核心**：AppModel.swift:149-193 signOut 单帧连写 ~20 个 @Observable 属性级联失效（局部构造再一次赋值）· RootView.swift:15 外部单例塞 @State · PlayerWindowFitter.swift:26-32,90-92 restore 被守卫跳过后新会话把贴完比例的帧当原始帧 · AppModel+Browser.swift:33-36 首页每次成功加载同步写 UserDefaults · AppModel+Playback.swift:366-370 dismissPlayer 收尾 fire-and-forget 无取消 · AppModel+Playback.swift:254-282 presentLocalFile/presentRequest 逐行相同可合并 · AppShell.swift:63-74,177-183 错误重试 UI 两份 + 常规布局死分支 · AppShell.swift:238-245 同 · DesignSystem.swift:1120-1146 死参数 reduceMotion · DesignSystem.swift:1017-1060 三处 rail 布局常量重复 · DanmakuModel.swift:24 dandanplayGatewayURL 零调用死代码 · LaunchOptions.swift:90-103 自检循环吞取消 · AppModel+Playback.swift:306,354,564 顶格错位缩进。

**播放器/详情**：DanmakuOverlay.swift:125-147 解析失败静默无日志 · PlayerScreen.swift:756-766 截图 toast 无 token · PlaybackReportingCoordinator.swift:90,119-125 triggeredTerminalRequestIDs 只增不减 · PlayerScreen.swift:742-754 shareURL 每 body stat 系统调用 · DetailView.swift:533 ForEach(id: \.self) 字符串重复冲突 · DetailView.swift:1050-1056 CancellationError 写进错误条 · DetailView.swift:113-115 刷新 Task 离页不取消 · DetailView.swift:139-158,185-218 骨架/真实渐变三份拷贝 · DetailView.swift:372-453,578-646 compact/regular 播放钮两套 · PlayerHUDDanmakuMenu.swift 死文件 · DanmakuSelectionSheet.swift:57-63 每 open 重搜无缓存 · PlaybackController+Screenshot.swift:27-29 文件名只滤 "/" · PlaybackController+Tracks.swift:149-153 fontSize 只更新 overlay（偏好三份镜像）·
【P2 阶段 1 复核残留（2026-08-29 晚，纯 SwiftUI 无法根治，需 UIKit 触点数桥接另行立项）】PlayerScreen 多指「冻结」变体：第二指落下后拖动既不 force-end 也不重启时，假 onEnded 不出现，去抖无从介入，seek 仍可能误落盘 · PlayerScreen 两指交替快触 <350ms 仍被判成双击暂停（与真双击在 SwiftUI 层不可区分）。

**浏览 UI**：LibraryView.swift:17-18,235-244 注释宣称淘汰旧页实际静默关分页 · LibraryView.swift:53-58 `count % pageSize == 0` 启发式提前终止分页 · BangumiSubjectDetailView.swift:47-49,470-476 章节三分 filter 每次 body 重算 6 遍（BangumiChapterSection.swift:46-50 同）· BangumiMatcher.swift:561-563 相似度分母用错基准 · AppUpdateChecker.swift:46-56 prerelease 截断与正式版同号混淆 · UpdateReleaseSheet.swift:50 整段 changelog 当本地化 key 查找 · SettingsView.swift:17 @State 包单例 · SettingsView.swift/OnboardingView.swift UserDefaults 前缀三套并存 · OnboardingView.swift:260-307 与 SettingsView.swift:194-239 SavedServerRow 两份 · OnboardingView.swift:262-270 AnyView 破坏 diffing · coverURL 助手 6 处复制 · 进度条 3 套实现 · BangumiHomeView.swift:801-810 状态文案与 CollectionType.description 重复维护 · BangumiHomeView.swift:255,270,313 退出搜索逻辑三处复制 · BangumiLinkPicker.swift:22-32 vs BangumiMatcher.swift:358-365 搜索词拼装两份 · MPSubscribeSheet 魔法串 "R"/"P"/"O" · MoviePilotHomeView.swift:352 右键「刷新」实为全量刷新 · MoviePilotHomeView.swift:475-481 目的地多余 .id · MoviePilotResourceView.swift:115 refreshable 不跟随流式 · BangumiHomeView.swift:655-657 翻页失败不上屏 · BangumiHomeView.swift:29,510-515 didAutoSync @State 视图重建再触发 · MoviePilotSubscribeSheet.swift:111-113 表单初值 .task 灌首帧闪占位 · HomeView.swift:54 无障碍文案写死「电视剧详情」。

**网络包**：BangumiDatabaseOperator.swift:86-100 逐类 COUNT×9 应 GROUP BY · :296-308 saveEpisodes 全捞回逐条比对可集合操作 · :317-333 NOT IN 占位符无上限可撞 999 变量上限 · DatabaseFactory.swift:30,69 索引建两次 + 空 foreign_keys pragma · BangumiSubject+Model.swift:138-170 每行解 7 个 JSON BLOB，高频列应提原生列 · Mapping.swift:104 + JellyfinServer.swift:211 id 缺失 UUID() 兜底身份抖动 · MoviePilotAPIClient.swift:10-17 信封双遍与 Emby 同款 · :73-79 init 直接改写入参 sessionConfiguration · BangumiAPIClient.swift:173-209 body: Any? 非 Sendable · ClientIdentity.swift:60-80 deviceID 读改写无锁 · JellyfinServer.swift:451-467 items() 翻页无上限 · MoviePilotSearch.swift:221-227 2xx 非 JSON 把成功当失败 · MoviePilotAPIClient.swift:335-339 `//` 拼接与 `+` 不转义 · Basis.swift:44-46 isExpired 无 leeway · BangumiURL.swift:160-161 全局命名污染 · JellyfinServer.swift:606-614 sendRaw 死代码。

**包与构建**：DanmakuAsyncLayer.swift:23 废弃十年的 OSAtomicIncrement32 · DanmakuQueuePool.swift:30-36 counter 无锁自增 · DanmakuAsyncLayer.swift:131-135 iOS opaque 分支双重缩放 · DanmakuView.swift:269-271 init?(coder:) 绕过初始化 · DanmakuView.swift:573-578 noSpaceSync 丢 cell 不回池与 shoot 不对称 · DanmakuTrack.swift:267,468-472 infinity 推 origin + 漏 leaveTrack 三处不对称 · ErikaEngine.swift:55,401 注释写 10s 实际 5s · fetch-erika.sh:93,173 提示文案指错路径 + zip-slip 无防护 · http-token-server.py:31-32 token 子串匹配放行。

## 五、与既有 backlog 对照（2026-08-24 轮）

| 既有 backlog 项 | 本轮状态 |
|---|---|
| AppModel 拆分 | 仍未拆；本轮给出具体缺口：41 属性、signOut 手工清 27 处、漏清 `playbackReporting` 一组 |
| 网络层去重 | 已有精确清单：错误类型×3、重试×2、单飞×2、URLSession 配置×3、UserDefaults store×5、日志 wrapper×3；建议合并顺序：错误类型 → 重试+单飞 → LockedDefaultsStore（省约 300 行）→ 删日志 wrapper |
| CI 缺口 | CoreModel 已补（34c2103）；新发现：ErikaReadAheadBehaviorTests 未 skip、无 SPM 缓存、release 不等 test |
| 本地化 | 仍为零；本轮另录「同一文案多处复制」若干 |
| Bangumi op/ed 子串误判 | 仍在；本轮另发现 hasSuffix("S"/"T") 更严重的整季误绑 |
| 401 处置不对称 | 仍在；新发现 Jellyfin 侧完全无 401 处置 |

## 六、建议处置顺序

1. **阶段 1 · 小修高收益（约 10 处单点修复）**：ATS 双 key（删 2 行）、switchToServer 死路、UInt32 clamp、DanmakuView.update、章节保底去重、找片下载接线、clearAccountState 接线、signOut 补清 playbackReporting、CI 补 skip、release needs:test。
2. **阶段 2 · 播放器体验**：iOS 手势三件套（中断收尾/多指守卫/panSession 失效范围）、时间偏移 UI、音量落盘节流、2x 不持久化、单击双击窗口、DetailView 缓存。
3. **阶段 3 · 结构性**：网络层去重（按上表顺序）、Emby sanitizer 按需化、Bangumi 建库出主 actor + WAL、MoviePilot 流式并发语义、FileHash 分块。
4. **阶段 4 · 清理**：CI 缓存、死代码（playNextEpisode、sendRaw、PlayerHUDDanmakuMenu、dandanplayGatewayURL）、重复组件收敛（SavedServerRow/coverURL/进度条/rail 常量）、版本号 xcconfig 单源。
